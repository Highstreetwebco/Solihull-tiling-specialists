const header=document.querySelector('.site-header'),menu=document.querySelector('.menu'),nav=document.querySelector('.site-header nav');
const syncHeader=()=>header.classList.toggle('scrolled',scrollY>innerHeight*.72);syncHeader();addEventListener('scroll',syncHeader,{passive:true});
menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}));
const slides=[...document.querySelectorAll('.hero-slide')],labels=['Outdoor living','Midnight suite','Peach onyx','Terraced garden'];let heroIndex=0,playing=true,heroTimer;
const showHero=i=>{slides[heroIndex].classList.remove('active');heroIndex=(i+slides.length)%slides.length;slides[heroIndex].classList.add('active');document.querySelector('#slide-current').textContent=String(heroIndex+1).padStart(2,'0');document.querySelector('#slide-label').textContent=labels[heroIndex]};
const playHero=()=>heroTimer=setInterval(()=>showHero(heroIndex+1),5500);playHero();
document.querySelector('.pause').addEventListener('click',e=>{playing=!playing;if(playing){playHero();e.currentTarget.textContent='Ⅱ';e.currentTarget.setAttribute('aria-label','Pause image sequence')}else{clearInterval(heroTimer);e.currentTarget.textContent='▶';e.currentTarget.setAttribute('aria-label','Play image sequence')}});
const cards=[...document.querySelectorAll('.gallery-card')],gallery=document.querySelector('.gallery'),progress=document.querySelector('.gallery-progress i');let projectIndex=0;
const showProject=i=>{projectIndex=(i+cards.length)%cards.length;const gap=18,left=cards[projectIndex].offsetLeft-cards[0].offsetLeft;gallery.style.transform=`translateX(-${left}px)`;cards.forEach((c,n)=>c.classList.toggle('active',n===projectIndex));progress.style.width=`${(projectIndex+1)*25}%`};
document.querySelector('.next').addEventListener('click',()=>showProject(projectIndex+1));document.querySelector('.prev').addEventListener('click',()=>showProject(projectIndex-1));gallery.addEventListener('keydown',e=>{if(e.key==='ArrowRight')showProject(projectIndex+1);if(e.key==='ArrowLeft')showProject(projectIndex-1)});addEventListener('resize',()=>showProject(projectIndex));
const reveal=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');reveal.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>reveal.observe(el));